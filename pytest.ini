[pytest]
# 让 pytest 把 src 目录加入模块搜索路径，
# 这样测试里可以写 from core.xxx import ...
pythonpath = src
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
