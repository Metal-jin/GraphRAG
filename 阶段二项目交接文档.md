# 金庸武侠知识图谱问答系统 —— 阶段二交接文档

> 适用：全体组员
> 版本：v1.0
> 状态：阶段一已完成，本文档定义**阶段二截止前**每个人的交付要求与验收标准

---

## 一、项目现状（阶段一已完成，达成共识）

- **定位**：武侠领域知识推理与可视化系统（**不是**通用方法对比平台）
- **数据**：《射雕英雄传》1789 个文本块 → 1664 个实体、3295 条关系（存 Neo4j）
- **检索**：3 个领域策略（师徒链 / 门派聚合 / 武功传承）+ 2 个基线（向量 / 调库）
- **评测**：50 道题，master_chain 准确率 **64%**，基线 12%
- **前端**：简单问答界面（方法下拉框 + 答案文本）
- **仓库**：`main` 分支已同步远程 `github.com/Metal-jin/GraphRAG`

---

## 二、阶段二总体目标（一句话）

把系统从"**能检索的问答工具**"升级为"**有领域推理能力 + 交互可视化体验的武侠知识系统**"。

三条主线：
1. **数据更深更全** —— 扩《神雕》《倚天》+ 实体对齐
2. **系统更智能** —— 意图自动识别 + 知识注入动态选择
3. **体验更好** —— 交互式关系图谱 + 传承树 + 时代过滤

---

## 三、截止前每人必须完成的交付物与验收标准

### 🔷 A+D（田惠今）—— 意图分类器 + 知识注入层 + 统筹

**必须完成：**
1. `intent_classifier.py`：自动识别问题类型（人物关系 / 武功传承 / 门派归属 / 剧情事件 / 综合比较）
2. 知识注入层：实现**三元组列表 / 关系路径 / 子图摘要**三种注入形式，并根据问题类型**自动选择**
3. 改造 `service.py`：`问题 → 意图识别 → 路由到策略 → 选注入形式 → 生成答案`
4. 更新 `INTERFACE.md` 接口约定

**验收标准（照此自测）：**
- 问「郭靖的师父的师父是谁」→ 自动走师徒链策略 + 路径注入
- 问「丐帮有哪些绝学」→ 自动走门派聚合 + 子图摘要注入
- **用户无需手动选方法**，系统自动路由

---

### 🔷 B（胡沛亮）—— 数据扩充 + 实体对齐

**必须完成：**
1. 扩充《神雕侠侣》《倚天屠龙记》数据，`build_kg.py` 支持多书、era 正确标注（射雕/神雕/倚天）
2. 实体对齐升级：从「手工别名表」升级为「规则 + 别名词典 + 相似度」的系统化对齐
3. 跑通 `clean_graph.py`，归并全部别名（九指神丐/北丐/洪帮主/七公 → 洪七公）
4. （P1 可选）新增「事件」实体类型

**验收标准：**

- 图谱覆盖三本书，era 属性正确
- 别名归并后，同一人物的多个称呼指向同一节点
- 跨书查询「黄衫女子」能关联到「杨过 / 小龙女」

---

### 🔷 C（刘鹏飞）—— 图算法 + 跨书关联

**必须完成：**
1. `graph_algorithms.py`：中心性（PageRank/度）、社区发现（Louvain）、最短路径
2. `cross_novel.py`：跨书人物关联检索策略
3. 输出「金庸宇宙核心人物排名」「势力社群划分」等分析结果

**验收标准：**
- 能算出任意两人物之间的「关系距离」（中间隔几跳）
- 能自动分出派系社群（全真 / 丐帮 / 桃花岛等）
- 跨书人物能正确关联（如杨过、小龙女、黄衫女子）

---

### 🔷 E1（陆彦达）—— 分类评测集 + 可解释性评测 + 注入对比实验

**必须完成：**
1. 评测集按问题类型分类（人物 / 武功 / 门派 / 综合）
2. `metrics.py`：在准确率之外，增加**路径完整性、证据充分性**等可解释性指标
3. 知识注入形式对比实验：三元组 vs 路径 vs 子图摘要，对 LLM 推理的影响

**验收标准：**
- 评测报告按问题类型给出**分项准确率**（不是一张总对比表）
- 有可解释性指标，不只报准确率
- 注入对比实验有明确结论（哪种形式对哪类问题更有效）

---

### 🔷 E2（周树寰）—— 交互式前端

**必须完成：**
1. 力导向关系图谱（D3.js / vis-network）：节点按类型着色，边按关系类型着色
2. 武功传承树（树状图）
3. 时代时间过滤（滑动条选时代，只显示该时期人物/门派）
4. 问答 + 图谱联动：答案自动定位图谱位置并高亮推理路径

**验收标准：**
- 点击人物能展开其关系网
- 多跳问题答案自动高亮整条推理路径
- 时间滑动条能过滤不同时代

---

## 五、协作约定（必须遵守）

1. **分支**：各自在 feature 分支开发，合并 `main` 前在群里说明
2. **提交**：commit 信息写清楚「做了什么」，不要写「改了一下」
3. **数据不进 git**：`data/source`、`data/interim` 已在 `.gitignore`，别强推
4. **密钥不进 git**：`.env` 严禁提交，只提交 `.env.example`（占位符）
5. **接口变更**：先改 `INTERFACE.md` 再改代码，避免别人被破坏

---

## 六、提醒

1. **扩充数据要花钱**：三本书全量抽取会消耗大量 LLM 调用，注意用量和预算
2. **Neo4j 数据要备份**：定期 `neo4j-admin dump` 导出，防止数据丢失
3. **Docker 依赖**：答辩/联调前确认 Docker Desktop 和 Neo4j 容器已启动

---

> 核心一句话：**阶段二截止前，每个人把自己那份「验收标准」全部跑通就算完成。** 联调时组长统一检查。
